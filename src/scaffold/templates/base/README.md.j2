# {{ project_name }}

{{ description }}

## Installation

```bash
uv sync
```

## Development

Run checks (pre-commit hooks via prek):

```bash
uv run prek run --all-files
```

Run tests:

```bash
uv run pytest
```
