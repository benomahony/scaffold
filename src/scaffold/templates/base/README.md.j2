# {{ project_name }}

{{ description }}

## Installation

```bash
uv sync
```

## Development

Run pre-commit hooks:

```bash
uv run pre-commit run --all-files
```

Run tests:

```bash
uv run pytest
```
